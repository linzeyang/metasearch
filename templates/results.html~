{% extends "base.html" %}

{% block title %}
Results for "{{ raw_query }}" -- MetaSearch
{% endblock %}

{% block content %}

    <form name="form1" id="form1" class="results" action="/search/" method="get" onsubmit="return validate();">
        <img src="/static/logo.gif" alt="logo image" />
        <input type="text" name="query" id="query" value="{{ raw_query }}" maxlength="100" onfocus="query_onfocus();" />
        <input type="submit" name="submit" id="submit" value="Search" /><br />
        <div id="checkboxes">
        <input type="checkbox" name="bing" id="bing" {% if showBing %}checked="checked" {% endif %}/><label for="bing">Bing</label>
        <input type="checkbox" name="blekko" id="blekko" {% if showBlekko %}checked="checked" {% endif %}/><label for="blekko">Blekko</label>
        <input type="checkbox" name="entweb" id="entweb" {% if showEntweb %}checked="checked" {% endif %}/><label for="entweb">EntireWeb</label><br />
        </div>
        <select name="aggr" id="aggr">
            <option value="false" selected="selected">Non-Aggregated</option>
            <option value="true">Aggregated</option>
        </select><br />
        <select name="cluster" id="cluster">
            <option value="false" selected="selected">Non-Clustered</option>
            <option value="true">Clustered</option>
        </select>
    </form>
    <br />
    {% for page in page_list %}
        <a href="{{ full_path }}&page={{ page }}">
        {% if page == current_page %}
            <b>[{{ page }}]</b>
        {% else %}
            {{ page }}
        {% endif %}
        </a>&nbsp;&nbsp;&nbsp;
    {% endfor %}
    <br />    
    {% if showBing %}
        <h3>BING</h3>
        <ul>
        {% for i in bingList %}
            <li><a href="{{ i.url }}">{{ i.title }}</a><br />
            {{ i.displayurl }}<br />
            <p>{{ i.snippet }}</p></li>
        {% endfor %}
        </ul>
        <hr />
    {% endif %}
    {% if showBlekko %}
        <h3>Blekko</h3>
        <ul>
        {% for i in blekkoList %}
            <li><a href="{{ i.url }}">{{ i.title|safe }}</a><br />
            {{ i.displayurl|safe }}<br />
            <p>{{ i.snippet|safe }}</p></li>
        {% endfor %}
        </ul>
        <hr />
    {% endif %}
    {% if showEntweb %}
        <h3>EntireWeb</h3>
        <ul>
        {% for i in entwebList %}
            <li><a href="{{ i.url }}">{{ i.title }}</a><br />
            {{ i.displayurl }}<br />
            <p>{{ i.snippet }}</p></li>
        {% endfor %}
        </ul>
    {% endif %}
    <br />
    {% for page in page_list %}
        <a href="{{ full_path }}&page={{ page }}">
        {% if page == current_page %}
            <b>[{{ page }}]</b>
        {% else %}
            {{ page }}
        {% endif %}
        </a>&nbsp;&nbsp;&nbsp;
    {% endfor %}
    <br />

{% endblock %}
